#include <bits/stdc++.h> 

using namespace std;

int main(){
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    
    freopen("파일이름.in", "r", stdin);
    scanf ("%d %d %d", &x, &y, &m); // 형식 수정 필요


    freopen("파일이름.out", "w", stdout);
    printf("%d\n", sum); // 형식 수정 필요

    return 0;
}